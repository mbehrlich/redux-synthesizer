import React from 'react';

const Recorder = (props) => {
  console.log(props);
  let start = "";
  let stop = "";
  // if (recording) {
  //   start = "disabled";
  // } else {
  //   stop = "disabled";
  // }
  return (
    <div>
      asdf
    </div>
  );
};

export default Recorder;
